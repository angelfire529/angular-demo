import { CreativesByCampaignPipe } from './creatives-by-campaign.pipe';

describe('CreativesByCampaignPipe', () => {
  it('create an instance', () => {
    const pipe = new CreativesByCampaignPipe();
    expect(pipe).toBeTruthy();
  });
});
